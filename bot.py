# bot.py
import os
from datetime import datetime, timedelta

from flask import Flask, request, abort

from linebot import LineBotApi, WebhookHandler
from linebot.exceptions import InvalidSignatureError
from linebot.models import MessageEvent, TextMessage, TextSendMessage

app = Flask(__name__)

# ====== LINE Config ======
CHANNEL_ACCESS_TOKEN = os.getenv("LINE_CHANNEL_ACCESS_TOKEN", "")
CHANNEL_SECRET = os.getenv("LINE_CHANNEL_SECRET", "")

# ถ้ายังไม่ตั้ง env ให้ใส่ตรงนี้ (ไม่แนะนำให้ใส่ token ลง github)
# CHANNEL_ACCESS_TOKEN = "ใส่-Channel-Access-Token"
# CHANNEL_SECRET = "ใส่-Channel-Secret"

if not CHANNEL_ACCESS_TOKEN or not CHANNEL_SECRET:
    print("⚠️ กรุณาตั้งค่า LINE_CHANNEL_ACCESS_TOKEN และ LINE_CHANNEL_SECRET ใน Environment")

line_bot_api = LineBotApi(CHANNEL_ACCESS_TOKEN)
handler = WebhookHandler(CHANNEL_SECRET)


# ====== Settings ======
TTS_LINK = "https://www.minimax.io/audio/text-to-speech"


# ====== Helpers ======
THAI_MONTHS = [
    "", "มกราคม", "กุมภาพันธ์", "มีนาคม", "เมษายน", "พฤษภาคม", "มิถุนายน",
    "กรกฎาคม", "สิงหาคม", "กันยายน", "ตุลาคม", "พฤศจิกายน", "ธันวาคม"
]

def thai_date(d: datetime) -> str:
    # พ.ศ.
    year_th = d.year + 543
    return f"{d.day} {THAI_MONTHS[d.month]} {year_th}"

def build_outage_template(date_text: str) -> str:
    # แบบ “ประกาศดับไฟ” ใกล้เคียงของเดิม (ปรับเองได้)
    return (
        "📢 ประกาศการงดจ่ายไฟฟ้าปฏิบัติงาน\n\n"
        "การไฟฟ้าส่วนภูมิภาคจังหวัดประจวบคีรีขันธ์\n"
        "จะดำเนินการปรับปรุงระบบจำหน่ายไฟฟ้า\n"
        "บริเวณ: ______________________________\n\n"
        f"🗓 วันที่: {date_text}\n"
        "⏰ เวลา: __:__ - __:__ น.\n\n"
        "จึงมีความจำเป็นต้องงดจ่ายกระแสไฟฟ้าชั่วคราว\n"
        "หากดำเนินการแล้วเสร็จก่อนกำหนด จะจ่ายไฟฟ้าทันที\n\n"
        "ขออภัยในความไม่สะดวก"
    )

def build_emergency_template() -> str:
    return (
        "⚠️ ประกาศการงดจ่ายไฟฟ้าฉุกเฉิน\n\n"
        "การไฟฟ้าส่วนภูมิภาคจังหวัดประจวบคีรีขันธ์\n"
        "ขณะนี้เกิดเหตุขัดข้องของไฟฟ้า\n"
        "เจ้าหน้าที่กำลังเร่งดำเนินการแก้ไขโดยด่วน\n\n"
        "ขออภัยในความไม่สะดวก"
    )


# ====== Routes ======
@app.route("/", methods=["GET"])
def home():
    return "OK", 200

@app.route("/callback", methods=["POST"])
def callback():
    signature = request.headers.get("X-Line-Signature", "")
    body = request.get_data(as_text=True)

    try:
        handler.handle(body, signature)
    except InvalidSignatureError:
        abort(400)

    return "OK", 200


# ====== Message Handler ======
@handler.add(MessageEvent, message=TextMessage)
def handle_message(event):
    user_text = (event.message.text or "").strip()

    # ---- คำสั่ง: ทำเสียงAI ----
    if "ทำเสียงAI" in user_text:
        reply_text = (
            "🔊 ทำเสียง AI อัตโนมัติ (Text to Speech)\n"
            "กดลิงก์นี้ได้เลย:\n"
            f"https://www.minimax.io/audio/text-to-speech\n\n"
            "✅ ทริค: คัดลอกข้อความประกาศจากบอท แล้วไปวางในเว็บเพื่อสร้างเสียง"
        )

    # ---- คำสั่ง: ประกาศดับไฟ (วันนี้) ----
    elif user_text in ["ดับไฟ", "ประกาศดับไฟ", "outage"]:
        today = thai_date(datetime.now())
        reply_text = build_outage_template(today)

    # ---- คำสั่ง: ประกาศดับไฟ (พรุ่งนี้) ----
    elif user_text in ["ดับไฟพรุ่งนี้", "ประกาศดับไฟพรุ่งนี้", "outage tomorrow"]:
        tomorrow = thai_date(datetime.now() + timedelta(days=1))
        reply_text = build_outage_template(tomorrow)

    # ---- คำสั่ง: ดับไฟฉุกเฉิน ----
    elif user_text in ["ดับไฟฉุกเฉิน", "ฉุกเฉิน", "emergency"]:
        reply_text = build_emergency_template()

    # ---- คำสั่ง: ให้ผู้ใช้พิมพ์ข้อความเอง ----
    # ตัวอย่าง: "ข้อความเอง: พรุ่งนี้ 9-12 ซอย..."
    elif user_text.startswith("ข้อความเอง:"):
        custom_text = user_text.replace("ข้อความเอง:", "", 1).strip()
        if not custom_text:
            reply_text = "พิมพ์ตามนี้ครับ\nข้อความเอง: (ใส่ข้อความประกาศที่ต้องการ)"
        else:
            reply_text = f"📝 ข้อความประกาศของคุณ:\n\n{custom_text}"

    # ---- help ----
    elif user_text.lower() in ["help", "ช่วยเหลือ", "คำสั่ง", "เมนู"]:
        reply_text = (
            "📌 คำสั่งที่ใช้ได้\n"
            "- ดับไฟ : เทมเพลตประกาศงดจ่ายไฟ (วันนี้)\n"
            "- ดับไฟพรุ่งนี้ : เทมเพลตประกาศงดจ่ายไฟ (พรุ่งนี้)\n"
            "- ดับไฟฉุกเฉิน : เทมเพลตประกาศเหตุขัดข้องฉุกเฉิน\n"
            "- ข้อความเอง: ... : ส่งข้อความที่คุณพิมพ์เอง\n"
            "- ทำเสียงAI : ส่งลิงก์ทำเสียงอัตโนมัติ\n"
        )

    else:
        reply_text = "พิมพ์ 'help' เพื่อดูคำสั่ง หรือพิมพ์ 'ดับไฟ' เพื่อสร้างประกาศ"

    line_bot_api.reply_message(
        event.reply_token,
        TextSendMessage(text=reply_text)
    )


if __name__ == "__main__":
    port = int(os.getenv("PORT", 5000))
    app.run(host="0.0.0.0", port=port)

